<%%
  require 'conjur/cli'
  require 'yaml'

  Conjur::Config.load
  Conjur::Config.apply
  api = Conjur::Authn.connect
-%>
<% @secrets.keys.sort.each do |secret_key| %>
  <%= secret_key %> = <%% api.variable <%= secret_key %> %>
<% end %>
